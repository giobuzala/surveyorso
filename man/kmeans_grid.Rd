% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/05_01_segmentation_kmeans_grid.R
\name{kmeans_grid}
\alias{kmeans_grid}
\title{\strong{Run a grid of k-means clustering models}}
\usage{
kmeans_grid(data, pc_range = NULL, k_range = 2:6, id_var = Vrid)
}
\arguments{
\item{data}{A data frame of numeric variables to cluster. Non-numeric columns are dropped.}

\item{pc_range}{Optional numeric vector of principal component counts. If \code{NULL}, no PCA is applied.}

\item{k_range}{Numeric vector of cluster counts to test. Defaults to \code{2:6}.}

\item{id_var}{ID variable to drop before clustering. Defaults to \code{Vrid}.}
}
\value{
A data frame summarizing results for each combination of PCs and clusters.
}
\description{
This function tests multiple k-means clustering solutions, optionally after applying principal component analysis (PCA) for dimensionality reduction.
\itemize{
\item Models are always run on the raw standardized data (no PCA).
\item If \code{pc_range} is provided, additional models are run using the first N principal components.
\item Results include cluster sizes, silhouette statistics, and within-cluster sum of squares (WSS).
\item Use \code{mean_sil} and \code{total_wss} to identify optimal cluster configurations.
}
}
\details{
The function automatically excludes the variable named "weight" from the clustering input.
}
\examples{
# K-means runs (2-5 k-range) with and without PCA (2-4 PCs range)
kmeans_results <- kmeans_grid(data = data_clust, pc_range = 2:4, k_range = 2:5)

}
