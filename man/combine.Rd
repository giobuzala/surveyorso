% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/08_02_misc_combine.R
\name{combine}
\alias{combine}
\title{\strong{Combine split variables into a single variable}}
\usage{
combine(data, var_1, var_2, new_name = NULL, new_label = NULL)
}
\arguments{
\item{data}{A data frame containing the variables to combine.}

\item{var_1}{First variable to merge.}

\item{var_2}{Second variable to merge.}

\item{new_name}{(Optional) Name for the new variable. If omitted, defaults to \code{var_1_var_2_COMB}.}

\item{new_label}{(Optional) SPSS-style variable label. If omitted, the label from \code{var_1} is copied.}
}
\value{
A modified data frame with a new combined variable placed immediately after \code{var_2}.
}
\description{
This function merges two variables that represent split versions of the same question (e.g., \code{Q1_1} asked to one group and \code{Q1_2} to another) into a single combined variable.
The function fills in missing values from one variable with non-missing values from the other, preserving factor levels and SPSS-style attributes.
}
\details{
\itemize{
\item If both variables have valid values for the same row, \code{var_1} takes precedence (a warning is issued).
\item Factor levels are inherited from \code{var_1}.
\item Existing variables with the same name are overwritten.
}

To apply this function across multiple pairs of split variables, you can map it using the code block below:

\preformatted{
pairs <- list(
  list("Q1_1", "Q1_2"),
  list("Q2_1", "Q2_2")
)

dat <- reduce(
  pairs,
  function(d, vars) {
    combine(d, !!sym(vars[[1]]), !!sym(vars[[2]]))
  },
  .init = dat
)
}
}
\examples{
# Combine Q1_1 and Q2_2
data <- combine(data = survey_data, var_1 = Q1_1, var_2 = Q1_2)

}
