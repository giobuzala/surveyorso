% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/07_03_open_ended_coding_theme_gpt.R
\name{theme_gpt}
\alias{theme_gpt}
\title{\strong{Generate a thematic code list using the OpenAI API}}
\usage{
theme_gpt(
  data,
  x,
  n = NULL,
  sample = NULL,
  model = "gpt-4o",
  instructions = NULL
)
}
\arguments{
\item{data}{A data frame containing the survey data.}

\item{x}{The open-ended variable to analyze.}

\item{n}{Integer; number of themes to return. Defaults to \code{10}.}

\item{sample}{Optional integer specifying the number of responses to sample for analysis. If \code{NULL}, all valid responses are used.}

\item{model}{Character string; the OpenAI model to use. Defaults to \verb{gpt-4o}.}

\item{instructions}{Optional string; additional instructions for coding.}
}
\value{
A table containing the generated thematic code list and their description. Standard codes (\code{Other}, \code{None}, \verb{Don't know}) are included automatically.
}
\description{
This function analyzes open-ended survey responses and automatically generates a set of thematic codes with descriptions.
Additional custom instructions can be supplied through the \code{instructions} argument.
}
\details{
Requires an OpenAI API key, which can be generated at \verb{https://platform.openai.com/}, to be set in your R session using \code{Sys.setenv(OPENAI_API_KEY="...")}.

The output is a tibble with three columns:
\itemize{
\item \code{Code}: A unique numeric code for each theme (standard codes 97–99 are added automatically).
\item \code{Bin}: Short label for the theme, written in sentence case.
\item \code{Description}: A one-sentence summary describing the theme's content.
}

Standard codes are included automatically:
\itemize{
\item \code{97} = \code{Other}
\item \code{98} = \code{None}
\item \code{99} = \verb{Don't know}
}

Use this function to create a \code{theme_list} for input into \code{code_gpt()}, or copy and paste it into an Excel coding workbook.

\strong{Note:} It’s best to review and refine the generated codes before using them in \code{code_gpt()}.
}
\examples{
# Generate a theme list for Q5
theme_list <- theme_gpt(data = survey_data, x = Q5)

}
