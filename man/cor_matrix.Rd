% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_04_statistical_cor_matrix.R
\name{cor_matrix}
\alias{cor_matrix}
\title{\strong{Compute a correlation matrix}}
\usage{
cor_matrix(
  data,
  weight = NULL,
  id_var = Vrid,
  method = "pearson",
  round = 3,
  half = TRUE,
  min = NULL
)
}
\arguments{
\item{data}{A data frame containing variables to correlate.}

\item{weight}{(Optional) Name of the weight variable. If \code{NULL}, computes unweighted correlations.}

\item{id_var}{ID column name to exclude (defaults to \code{Vrid}).}

\item{method}{Correlation method (\verb{pearson"}, \code{"spearman"}, or \code{"kendall"}).}

\item{round}{Number of decimal places to round correlations to.}

\item{half}{Logical; if \code{TRUE} (default), displays only the lower triangle. If \code{FALSE}, displays the full matrix.}

\item{min}{(Optional) Numeric threshold. If specified, correlations with |r| < min are replaced with NA.}
}
\value{
A correlation matrix, as a data frame.
}
\description{
This function computes a correlation matrix across all numeric variables in a dataset.
}
\details{
Only numeric variables are allowed. Non-numeric variables (e.g., factors, characters, or dates) will trigger an error.
}
\examples{
# Weighted Pearson (default) correlations, lower half of the matrix shown
cor_matrix(data = data_clust, weight = weight_var)

# Unweighted Spearman correlations, full matrix shown, with minimum threshold of 0.3
cor_matrix(data = data_clust, method = "spearman", half = FALSE, min = 0.3)

}
