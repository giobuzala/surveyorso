% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/02_04_tabulation_tab_mr_all.R
\name{tab_mr_all}
\alias{tab_mr_all}
\title{\strong{Apply \code{tab_mr()} to multiple multiple response sets}}
\usage{
tab_mr_all(data, y = NULL, weight = NULL, exclude = NULL, ...)
}
\arguments{
\item{data}{A data frame containing the survey data.}

\item{y}{(Optional) A variable to cross-tabulate against a multiple response set. Defaults to \code{NULL}.}

\item{weight}{(Optional) A numeric weighting variable. If \code{NULL} (default), results are unweighted.}

\item{exclude}{Variables to exclude from tabulation. Defaults to \code{NULL}. Always excludes \code{y} (if specified), \code{weight} (if specified), and variable literally named "weight".}

\item{...}{Additional arguments passed to \code{tab_mr()}.}
}
\value{
A named list of frequency tables or crosstabs, one per multiple response set prefix.
}
\description{
This function maps \code{tab_mr()} across all multiple response set question prefixes in a data frame.
Optionally, you can specify a \code{y} variable for crosstabs and/or exclude prefixes from being tabulated.
Additional arguments are passed to \code{tab_mr()}.

Multiple response sets are detected automatically by prefixes before the first underscore (\verb{_}).
}
\examples{
# Unweighted frequency tables of all multiple response sets
tab_mr_all(data = survey_data)

# Weighted crosstabs of all multiple response sets by region, excluding Q99
tab_mr_all(data = survey_data, y = region, weight = weight_var, exclude = "Q99")
}
