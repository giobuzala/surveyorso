# **Custom print method for `tab_mr()` results**

Provides a readable console summary for
[`tab_mr()`](https://giobuzala.github.io/surveyorso/reference/tab_mr.md)
output by displaying a descriptive header followed by the table.

## Usage

``` r
# S3 method for class 'tab_mr_result'
print(x, ...)
```
